[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "todo"
version = "0.1.0"
dependencies = [
  "xdg-base-dirs == 6.0.2"
]

[project.scripts]
todo = "todo.main:main"
